<%= render 'admin/link' %>

<h2>会員一覧</h2>

  <table>
    <thead>
      <tr>
        <th>会員ID</th>
        <th>氏名</th>
        <th>メールアドレス</th>
        <th>ステータス</th>
      </tr>
    </thead>

    <% @customers.each do |user| %>

    <tbody>
      <tr>
        <td><%= customer.id %></td>
        <td><%= customer.last_name.first_name %></td>
        <td><%= customer.email %></td>
        <td></td>
      </tr>
    </tbody>

    <% end %>

  </table>
